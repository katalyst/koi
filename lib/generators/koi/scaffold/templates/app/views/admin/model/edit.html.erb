<% content_for :title, "Edit HUMAN_NAME" %>
<% content_for(:breadcrumbs) do %>
  <%= link_to "HUMAN_PLURAL_NAME", [:admin, CLASS_NAME] %>
  <%= link_to SINGULAR_NAME.id, [:admin, SINGULAR_NAME] %>
<% end %>

<%= form_with(model: SINGULAR_NAME, url: [:admin, SINGULAR_NAME]) do |form| %>
  <%= render "form_fields", form: %>

  <div class="actions">
    <%= form.admin_save %>
  </div>
<% end %>
