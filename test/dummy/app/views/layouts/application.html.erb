<!DOCTYPE html>
<html>
  <head>
    <meta http-equiv="content-type" content="text/html;charset=UTF-8" />
    <title><%= t("site.title", default: "Default Title") %></title>
    <meta http-equiv="description" content="<%= t("site.meta_description", default: "") %>" />
    <meta http-equiv="keywords" content="<%= t("site.meta_keywords", default: "") %>" />
    <%= stylesheet_link_tag    "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <%= raw t("site.google_analytics_embed", default: "") %>
  </head>
  <body>

  <header>
    <%= koi_render_navigation "main_menu_header_navigation", "header_navigation", renderer: :sf_menu, level: 1..3, expand_all: true, dom_id: "nav-one", dom_class: 'nav' %>
  </header>

  <p><%= koi_render_navigation "breadcrumbs", nil, renderer: :breadcrumbs, join_with: ' &raquo; ' %></p>
  <p><%= koi_render_navigation "active_nav_item", nil, renderer: :active_items %></p>

  <%= yield :title if content_for?(:title) %>
  <%= yield %>

  <footer>
    <%= koi_render_navigation "footer_navigation", "footer_navigation", expand_all: true %>
  </footer>

  </body>
</html>
