en:
  time:
    formats:
      default: "%Y-%m-%dT%H:%M:%S.%L%z"
      display: "%e %B %Y, %l:%M%P"
  date:
    formats:
      default: "%Y-%m-%d"
      display: "%e %B %Y"
  koi:
    labels:
      new: New
      search: Search
  helpers:
    hint:
      default:
        document: Must be less than %{max_size}
        image: Must be less than %{max_size}
