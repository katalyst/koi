<% content_for :header do %>
  <%= render(PageHeaderComponent.new(title: "New admin user")) do |c| %>
    <% c.with_breadcrumb("Admin users", admin_admin_users_path) %>
  <% end %>
<% end %>

<%= form_with(model: admin, url: admin_admin_users_path) do |form| %>
  <%= render "form_fields", form: %>

  <div class="actions">
    <%= form.admin_save %>
  </div>
<% end %>
