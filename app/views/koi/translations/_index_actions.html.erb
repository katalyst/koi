<div class="listing--actions small">
  <% if respond_to? :parent %>
    <%= link_to "Back", collection_path, class: "button button__small align-box-left" %>
  <% end %>
  <div class="align-box-right f">
    <div class="button-group">
      <%- if is_allowed?(:new) && current_admin.god? %>
        <div>
          <%= link_to "Create", new_resource_path, class: "button__confirm button__small" %>
        </div>
      <% end %>
    </div>
  </div>
</div>
