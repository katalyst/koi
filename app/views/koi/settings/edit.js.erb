var $modal = $("<%= escape_javascript render("modal_setting") %>");
var popupOptions = $.extend({}, Ornament.popupOptions);
popupOptions.items = {
  src: $modal
};
$.magnificPopup.open(popupOptions);
