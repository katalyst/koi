$("#modal-for-extra").html ("<%= escape_javascript render("modal_setting") %>");
$.magnificPopup.open({
  items: {
    src: $("#modal-for-extra")
  },
  callbacks: {
    open: function(){
      $(document).trigger("ornament:ck-editor")
    }
  }
});