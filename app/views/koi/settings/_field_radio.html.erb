<% data = object.data_source.call if object.data_source %>
<% if data.nil? %>
  <%= f.input attr.to_sym, :as => :radio, label: label %>
<% else %>
  <%= f.input attr.to_sym, :as => :radio, collection: data, label: label %>
<% end %>
