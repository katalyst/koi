<% data = object.data_source.call if object.data_source %>
<% if data.nil? %>
  <%= f.input :serialized_value, :as => :check_boxes, label: label %>
<% else %>
  <%= f.input :serialized_value, :as => :check_boxes, collection: data, label: label %>
<% end %>

<%= render "koi/admin_crud/form_field_collection_errors", f: f, attr: attr %>
