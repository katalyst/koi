<div class="option">
  <% html_id = new_uuid %>
  <% checked = defined?(checked) && checked  %>
  <%= radio_button_tag "size", title, checked, :"data-href" => resource.url(:size => size), id: html_id %>
  <%= label_tag html_id, title %>
</div>
