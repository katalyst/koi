<% data = resource_class.crud.find(:fields, attr, :data) %>
<% if data.nil? %>
  <%= f.input attr.to_sym, :as => :radio %>
<% else %>
  <%= f.input attr.to_sym, :as => :radio, collection: data %>
<% end %>

