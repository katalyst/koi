<%- if @current_path && !@current_path.title.blank? %>
  <%- content_for :title, @current_path.title -%>
<%- else %>
  <%- content_for :title, resource_class.crud.find(:admin, :title, :show) || resource -%>
<%- end %>