<% data = resource_class.crud.find(:fields, attr, :data) %>
<% if data.nil? %>
  <%= f.input attr.to_sym, :as => :check_boxes %>
<% else %>
  <%= f.input attr.to_sym, :as => :check_boxes, collection: data %>
<% end %>

<%= render "koi/admin_crud/form_field_collection_errors", f: f, attr: attr %>