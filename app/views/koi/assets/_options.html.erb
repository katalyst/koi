<%
=begin %>

The default is no options!

The only thing asset injection requires is the url which is provided by the form in show.html.erb.

For Images this is customized by providing different sizes.

<%
=end %>
