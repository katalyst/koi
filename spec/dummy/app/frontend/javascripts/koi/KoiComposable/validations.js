/*

  Custom Composable Validation Rules

  See wiki for details:
  https://github.com/katalyst/koi/wiki/Composable:-Validations

*/

/*
const exampleValidation = (props) => {
  const { value } = props;
  if(props !== "example") {
    return "Must be 'example'";
  }
}
*/

// Export an object of named validations
// {
//  "myValidation": validationFunction,
//  "myOtherValidation": otherValidationFunction,
// }
export default {
  // "example": exampleValidation,
};